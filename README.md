HELLO

This Project still WORK IN PROGRESS

Welcome to my first Portoflio

in this repository, i'm gonna used golang as the backend languange
Why I choose golang because in other reason im learning golang also golang is the best for modern app
from the worker if the application going to scale by default golang have been built in worker not like
excluded worker thread and i'm making this at first microservice ready

LINT:
- golangci-lint run

SWAGGER:
- swag init (regenerate) after update the handler
    EX Route API DOC:
    http://localhost:{PORT}/swagger/index.html

THE FLOW API:
- Main -> Module -> Handler + DTO -> Service + DTO -> Repository (DB + Model)